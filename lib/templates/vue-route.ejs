// 普通加载路由
// import <%= data.name %> from '<%= data.path %>.vue'

// 懒加载路由
const <%= data.name %> = () => import('<%= data.path %>.vue')
export default {
  path: '/<%= data.lowerName %>',
  name: '<%= data.name %>',
  component: <%= data.name %>,
  children: []
}
